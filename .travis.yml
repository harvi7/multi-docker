language: generic 
sudo: required
services:
    -docker

before_install:
    - docker build -t harsh/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true harsh/react-test npm test

after_success:
    - docker build -t harshvirani/multi-client ./client
    - docker build -t harshvirani/multi-nginx ./nginx
    - docker build -t harshvirani/multi-server ./server
    - docker build -t harshvirani/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push harshvirani/multi-client
    - docker push harshvirani/multi-nginx
    - docker push harshvirani/multi-server
    - docker push harshvirani/multi-worker